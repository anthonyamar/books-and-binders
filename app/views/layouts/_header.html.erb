<nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
  <a class="navbar-brand" href="#">MyLibrary</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto"></ul>
    <form class="form-inline my-2 my-lg-0">
      <% if user_signed_in? %>
        <%= link_to "Mon profil", edit_user_registration_path, class: "btn btn-outline-info my-2 my-sm-0 mr-2" %>
        <%= link_to "Deconnexion", destroy_user_session_path, method: :delete, class: "btn btn-outline-danger my-2 my-sm-0" %>
      <% else %>
        <%= link_to "Connexion", new_user_session_path, class: "btn btn-outline-success my-2 my-sm-0" %>
      <% end %>
    </form>
  </div>
</nav>